# tests/

Tests for the Authority Compiler.

Tests validate the compiler against the specifications in `specs/`. Because each
pipeline stage is separable, tests are expected to cover stages in isolation as
well as the pipeline end to end.
